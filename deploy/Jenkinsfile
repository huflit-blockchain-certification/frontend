pipeline {
    agent any
    stages {
        stage('Deployy') {
            steps {
                sshagent(['13.250.7.241']) {
                    sh '''
                        cd /home/blockchain/frontend &&
                        git pull &&
                        docker-compose up -d --build
                        "
                    '''
                }
            }
        }
    }
}
